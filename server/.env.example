# ============================================
# ARC RAIDERS COMPANION - ENVIRONMENT VARIABLES
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================

# Option 1: Local Development (Individual variables)
# Use these for local PostgreSQL development
DB_HOST=localhost
DB_PORT=5432
DB_NAME=arcraiders
DB_USER=postgres
DB_PASSWORD=your_password_here

# Option 2: Production (Connection String)
# Use DATABASE_URL for cloud databases (Heroku, AWS RDS, Railway, Render, etc.)
# If DATABASE_URL is set, it takes priority over individual DB_* variables above
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name

# Examples of DATABASE_URL for different providers:
# ------------------------------------------------
# AWS RDS:
# DATABASE_URL=postgresql://admin:mypassword@mydb.us-east-1.rds.amazonaws.com:5432/arcraiders

# Heroku (automatically set by Heroku):
# DATABASE_URL=postgres://user:pass@ec2-xx-xxx-xxx-xx.compute-1.amazonaws.com:5432/dbname

# Railway (automatically set by Railway):
# DATABASE_URL=postgresql://postgres:password@containers-us-west-xx.railway.app:5432/railway

# Render (automatically set by Render):
# DATABASE_URL=postgres://user:pass@dpg-xxxxx-a.oregon-postgres.render.com/dbname

# Neon (serverless Postgres):
# DATABASE_URL=postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb

# Supabase:
# DATABASE_URL=postgresql://postgres:[PASSWORD]@db.xxx.supabase.co:5432/postgres


# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate a secure random string (32+ characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production


# ============================================
# SERVER CONFIGURATION
# ============================================
PORT=5001
NODE_ENV=development
# In production, set NODE_ENV=production


# ============================================
# EMAIL CONFIGURATION
# ============================================
# In development, emails are logged to console
# In production (NODE_ENV=production), emails are sent via SMTP

# For production, uncomment and configure ONE of the options below:

# --- Option A: Gmail ---
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-16-char-app-password
# EMAIL_FROM="ARC Raiders Companion <your-email@gmail.com>"

# --- Option B: SendGrid (Recommended) ---
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=apikey
# EMAIL_PASSWORD=SG.your-sendgrid-api-key
# EMAIL_FROM="ARC Raiders Companion <noreply@yourdomain.com>"

# --- Option C: AWS SES ---
# EMAIL_HOST=email-smtp.us-east-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=your-smtp-username
# EMAIL_PASSWORD=your-smtp-password
# EMAIL_FROM="ARC Raiders Companion <verified@yourdomain.com>"

# --- Option D: Mailgun ---
# EMAIL_HOST=smtp.mailgun.org
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=postmaster@your-domain.mailgun.org
# EMAIL_PASSWORD=your-mailgun-password
# EMAIL_FROM="ARC Raiders Companion <noreply@yourdomain.com>"


# ============================================
# FRONTEND URL
# ============================================
# Used for email verification links
# Development:
FRONTEND_URL=http://localhost:3002

# Production examples:
# FRONTEND_URL=https://arcraiders-companion.vercel.app
# FRONTEND_URL=https://your-app.netlify.app
# FRONTEND_URL=https://yourdomain.com


# ============================================
# NOTES
# ============================================
# 
# 1. NEVER commit this file to Git! It's in .gitignore
# 2. Copy this to .env and fill in your actual values
# 3. For production, use environment variables in your hosting platform
# 4. DATABASE_URL takes priority over individual DB_* variables
# 5. Generate JWT_SECRET with: openssl rand -base64 32
# 6. Use App Passwords for Gmail, not your regular password
# 7. Set NODE_ENV=production to enable email sending
#
